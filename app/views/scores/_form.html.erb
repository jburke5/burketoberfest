<%= form_for([@game,@game.scores.build]) do |f| %>
	<div>
	    <%= f.label :player %><br>
	    <%= f.text_field :player %>
	  </div>

	  <div>
	    <%= f.label :rank %><br>
	    <%= f.text_area :rank %>
	  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
